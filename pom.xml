<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
	<modelVersion>4.0.0</modelVersion>

	<groupId>com.rsmart.kuali</groupId>
	<artifactId>cx_data_migration</artifactId>
	<version>5.2.1.4</version>

	<prerequisites>
		<maven>3.2.1</maven>
	</prerequisites>
	<properties>
		<project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
		<spring.version>3.1.2.RELEASE</spring.version>
		<jersey.version>1.18</jersey.version>
		<slf4j.version>1.6.6</slf4j.version>
		<java.version>1.6</java.version>
		<junit.version>4.10</junit.version>
	    <mysql.version>5.1.16</mysql.version>
		<wagon.s3.version>1.2.1</wagon.s3.version>
		<jdk.source.version>1.6</jdk.source.version>
		<jdk.target.version>1.6</jdk.target.version>
	</properties>


	<repositories>
		<!--rSmart Nexus -->
		<repository>
			<id>rsmart-dev</id>
			<name>rsmart-dev</name>
			<url>https://nexus.rsmart.com/nexus/content/groups/public/</url>
			<snapshots>
				<enabled>true</enabled>
			</snapshots>
			<releases>
				<enabled>true</enabled>
			</releases>
		</repository>
	</repositories>

	<!-- rSmart-Nexus -->
	<distributionManagement>
		<snapshotRepository>
			<id>rsmart.snapshot.repo</id>
			<name>rsmart snapshot repository</name>
			<url>s3://rsmart-dev/artifacts/maven/snapshot</url>
		</snapshotRepository>
		<repository>
			<id>rsmart.release.repo</id>
			<name>rsmart release repository</name>
			<url>s3://rsmart-dev/artifacts/maven/release</url>
		</repository>
	</distributionManagement>

	<dependencies>

		<dependency>
			<groupId>com.googlecode.flyway</groupId>
			<artifactId>flyway-core</artifactId>
			<version>2.3.1</version>
		</dependency>

		<dependency>
			<groupId>org.kuali.kra</groupId>
			<artifactId>kc_project</artifactId>
			<version>5.1.1.2-rsmart</version>
			<type>jar</type>
			<classifier>classes</classifier>
			<scope>provided</scope>
		</dependency>

		<dependency>
			<groupId>org.kuali.rice</groupId>
			<artifactId>rice-core-web</artifactId>
			<version>2.1.7.3-rsmart</version>
			<scope>provided</scope>
		</dependency>

		<!-- Test Dependencies -->

		<dependency>
			<groupId>junit</groupId>
			<artifactId>junit</artifactId>
			<version>${junit.version}</version>
			<scope>test</scope>
		</dependency>

		<dependency>
			<groupId>org.mockito</groupId>
			<artifactId>mockito-core</artifactId>
			<version>1.9.5</version>
			<scope>test</scope>
		</dependency>

		<dependency>
			<groupId>org.springframework</groupId>
			<artifactId>spring-core</artifactId>
			<version>3.2.3.RELEASE</version>
			<scope>provided</scope>
		</dependency>
	</dependencies>

	<build>

		<resources>
			<resource>
				<directory>src/main/resources</directory>
				<filtering>true</filtering>
			</resource>
		</resources>

		<plugins>
			<plugin>
				<groupId>org.apache.maven.plugins</groupId>
				<artifactId>maven-compiler-plugin</artifactId>
				<version>3.1</version>
				<configuration>
					<source>${jdk.source.version}</source>
					<target>${jdk.target.version}</target>
				</configuration>
			</plugin>
		</plugins>
		<extensions>
			<extension>
				<groupId>org.kuali.maven.wagons</groupId>
				<artifactId>maven-s3-wagon</artifactId>
				<version>${wagon.s3.version}</version>
			</extension>
		</extensions>
	</build>

	<profiles>
	</profiles>

  <reporting>
    <plugins>
      <plugin>
        <groupId>org.codehaus.mojo</groupId>
        <artifactId>cobertura-maven-plugin</artifactId>
        <version>2.6</version>
      </plugin>
    </plugins>
  </reporting>

	<!-- rsmart-nexus -->
  <scm>
    <connection>scm:git:git://github.com/rSmart/cx_data_migration.git</connection>
    <developerConnection>scm:git:git@github.com:rSmart/cx_data_migration.git</developerConnection>
    <url>https://github.com/rSmart/cx_data_migration</url>
    <tag>cx_data_migration-5.2.1.4</tag>
  </scm>
</project>
