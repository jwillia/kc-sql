{
  "name": "prepend_flyway_version",
  "version": "0.0.1",
  "description": "Prepend supplied sql files with a version number based on the date",
  "main": "prepend_version.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "AGPL",
  "dependencies": {
    "dateformat": "^1.0.11",
    "lazy": "^1.0.11",
    "path": "^0.11.14",
    "sculpt": "^0.1.6",
    "split": "^0.3.2"
  }
}
